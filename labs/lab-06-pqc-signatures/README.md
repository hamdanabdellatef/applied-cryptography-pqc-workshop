# Lab 6: ML-DSA Signatures

Python exercise workspace. Implemented and verified locally; live Colab delivery check pending.

- `starter.py`: generated standalone experiments, learner function, acceptance checks and labelled reference. Make a learner copy before editing; regeneration overwrites this file.
- Student notebook: `notebooks/lab-06-pqc-signatures.ipynb` at the repository root.
- Lab manual: `docs/labs/lab-06-pqc-signatures.md` at the repository root.

ML-DSA signing, tamper detection, and comparison with Ed25519.

Install `requirements-day2.txt` from the repository root, then run this starter with the workshop Python. The untouched learner function reports NOT ATTEMPTED while reference checks run separately. Author the manual and shared `src/crypto_workshop/day2.py`, then run `scripts/build_day2.py`. Do not treat reference success as learner completion.
