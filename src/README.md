# Shared Python code

Place reusable, tested lab helpers in `crypto_workshop/`. Keep intentionally vulnerable exercises inside their specific lab directory. Select packaging and distribution for Colab when the first lab is implemented.
